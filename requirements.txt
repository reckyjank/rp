runpod
transformers==4.30.0         # For working with Hugging Face models
diffusers==0.18.0            # For loading the image generation model (like Stable Diffusion)
torch==2.1.0                 # Required for running models like Stable Diffusion (PyTorch backend)
Pillow==9.1.1                # For image processing (e.g., converting to base64)
